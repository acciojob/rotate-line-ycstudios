const line=document.getElementById('line')
let angle=0
function rotation(){
    angle+=2;
    line.style.transform=`rotate(${angle}deg)`;
    
}
setInterval(rotation, 20);//your JS code here. If required.
const line=document.getElementById('line')
let angle=0
function rotation(){
    angle+=2;
    line.style.transform=`rotate(${angle}deg)`;
    
}
setInterval(rotation, 20);